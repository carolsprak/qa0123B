Skip to content
carolsprak
/
qa1222
Public
Code
Issues
Pull requests
More
Node.js CI - anne
Update sum.js #5
Workflow file for this run
.github/workflows/node.js.yml at 3c34c1a
name: Node.js CI - anne

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'
      - run: npm install
      - run: npm run build --if-present      

  unit_test:
    runs-on: ubuntu-latest

    needs: [ build ]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18.x'    
      - run: npm install
      - run: npm test
Update sum.js · carolsprak/qa1222@3c34c1a
